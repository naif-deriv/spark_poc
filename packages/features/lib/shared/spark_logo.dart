import 'package:flutter/material.dart';

class SparkLogo extends StatelessWidget {
  const SparkLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/spark_logo.png'),
    );
  }
}
